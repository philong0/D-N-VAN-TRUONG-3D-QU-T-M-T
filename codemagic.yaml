workflows:
  ios-scanner:
    name: Dr Van Truong 3D Scanner iOS App
    max_build_duration: 30
    instance_type: mac_mini_m2
    environment:
      xcode: latest
      cocoapods: default
      vars:
        XCODE_PROJECT: "ios/DrVanTruongScanner.xcodeproj"
        XCODE_SCHEME: "DrVanTruongScanner"
    scripts:
      - name: Build and Package iOS IPA
        script: |
          set -e
          echo "=== 1. Building Xcode Release Archive ==="
          xcodebuild archive \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$PWD/build/DrVanTruongScanner.xcarchive" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

          echo "=== 2. Creating IPA Package ==="
          mkdir -p "$PWD/build/Payload"
          cp -r "$PWD/build/DrVanTruongScanner.xcarchive/Products/Applications/DrVanTruongScanner.app" "$PWD/build/Payload/"
          
          cd "$PWD/build"
          zip -r "DrVanTruongScanner.ipa" Payload
          cp "DrVanTruongScanner.ipa" "$CM_BUILD_DIR/" 2>/dev/null || true
          cd "$CM_BUILD_DIR"

          echo "=== 3. Verify Artifacts Created ==="
          ls -la "$PWD/build/"
          ls -la "$PWD/build/Payload/"
    artifacts:
      - build/*.ipa
      - build/**/*.ipa
      - "*.ipa"
      - build/DrVanTruongScanner.xcarchive
      - build/**/*.app
